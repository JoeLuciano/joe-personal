import { motion } from 'framer-motion';
import styles from './AccountForm.module.css';

export const AccountForm = ({ isMobile, data }) => {
  return <motion.div className={styles['postcard']}>test</motion.div>;
};
