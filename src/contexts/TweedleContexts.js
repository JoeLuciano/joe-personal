import { createContext } from 'react';

export const GuessObjectsContext = createContext(null);
export const GuessesContext = createContext(null);
export const HandleOnScreenKeyboardChangeContext = createContext(null);
