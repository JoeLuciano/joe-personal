import { motion } from 'framer-motion';
import { LoginForm } from 'components/pageComponents/userComponents/formComponents/login/LoginForm';

export const LoginPage = () => {
  return (
    <motion.div>
      <LoginForm />
    </motion.div>
  );
};
